[Service]
# Redundant flood path: passive while the primary LoRaWAN network answers,
# take over only after the device shows nobody else is responding.
Environment=OTAA_ANSWER_JOINS=auto
Environment=JOIN_FAILOVER_COUNT=3
Environment=JOIN_FAILOVER_WINDOW=1800
